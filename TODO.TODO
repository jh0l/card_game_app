TODO:
    [ ] add image definition list
        [ ] new image
        [ ] rename existing image
        [ ] replace image source for an existing image definition        
        [ ] delete an image
        [ ] alias an image definition to another image definition?
        [ ] upload images in bulk

    [ ] create card definition template dialog form based on 'Set Card Type' combobox
    [ ] edit card definition templates
        [ ] add a card type template
        [ ] edit card type template name
        [ ] add/delete/change card type template values
            [ ] add/delete/change the (text) properties of the card type
                [ ] the name of the property
                [ ] enableable/disableable default values for (empty input means dont set value or if none on the target set to '')
                    [ ] position [each value]
                    [ ] rotation [each value]
                    [ ] text alignment [each value]
                    [ ] size
                    [ ] width
                    [ ] scale
                    [ ] R.Order -/+
            [ ] add/delete/change the graphics of the card type
                [ ] the label of the graphic
                [ ] the graphic definition of the property
                    will need to create the graphic definition if it doesn't exist on the client????
                [ ] enableable/disableable default values for
                    [ ] position (each value)
                    [ ] rotation (each value)
                    [ ] scale, R.Order -/+, Index (make implicit?)
                    
        [ ] delete card type templates
        [ ] apply template values to a selection of card definitions
        
    [ ] when card property deviates from template value, show difference in popover somehow? 
         
create card from text
